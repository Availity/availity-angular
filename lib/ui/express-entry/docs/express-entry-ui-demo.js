(function(root) {
  'use strict';

  var availity = root.availity;

  availity.demo.controller('ExpressEntryController', function($scope) {
    $scope.content = "Some content to render LOL";
  });
})(window);
